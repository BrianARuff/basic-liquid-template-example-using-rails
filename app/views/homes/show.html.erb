<h1>User Pages</h1>
<ul>
  <% @pages.each do |page| %>
    <li><%= link_to "#{page.user}'s page", page_path(page) %></li>
  <% end %>
</ul>

<h2>These Objects were created in the seeds.rb file using liquid and then rendered by the embedded ruby </h2>

<%= image_tag 'liquid.png', class: 'img-responsive' %>